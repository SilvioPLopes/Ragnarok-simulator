services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ragnarok_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgre
    ports:
      - "5432:5432"
    volumes:
      - ragnarok_data:/var/lib/postgresql/data

volumes:
  ragnarok_data:
