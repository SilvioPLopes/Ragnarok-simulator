Act as a Senior Java Backend Developer. Fix the 'item_name' mapping in the NPC Shop response. Currently, the DTO (likely NpcShopResponseDTO) pulls the dummy 'item_name' (e.g., 'Item 519') directly from the 'npc_shop_items' table. You must change this to pull the real 'name' from the main 'items' table using the 'item_id' relationship. Use the exact same logic pattern already implemented for the 'price' field, which successfully looks up the 'items' table when the shop price is -1. Trace the flow from NpcShopResponseDTO, update the name mapping, and do not alter the existing price or image logic. Stop and show the code.